import styled from 'styled-components';

export const ContainerFilter = styled.div`
    display: grid;
    grid-template-columns: 1fr 1fr;
    width: 100%;
    height: 30vh;
`;

export const FilterClient = styled.div`
    display: flex;
    flex-direction: column;
    align-items: space-between;
    justify-content: center;
`;

export const FilterClientText = styled.div`
    
`;

export const FilterClientSearch = styled.div`

`;

export const FilterIndice = styled.div`

`;

export const FilterIndiceText = styled.div`

`;

export const FilterIndiceOptions = styled.div`

`;
